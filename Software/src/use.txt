# 1. 回到 Workspace 根目錄
cd /home/aiRobots/Software

# 2. 載入環境變數 (這步讓系統知道你的 robot_core 在哪)
source devel/setup.bash

# 3. 使用 rosrun 執行 (推薦)
# 格式: rosrun [Package名稱] [執行檔名稱]
# 因為你的 ProjectNode 是在最外層定義的，它通常會歸屬於 "Project" 這個 package (看你最外層 CMakeLists 的 project 名稱)
rosrun Project ProjectNode


source devel/setup.bash
rosservice list
# 如果你看到列表裡面出現： /batch_transform




# copilot 更新檔
ctrl+alt+i

我想要更新這個資料夾當中有用git追蹤的檔案（沒有上傳追蹤的不要更新上傳）




# 1. 開啟第一個視窗 開啟roscore
cd Software
source devel/setup.bash
roscore
# 2. 開啟第二個視窗 執行底層手臂運做
cd Software
source devel/setup.bash

# 3. 確認手臂姿態是否正常？（左手線朝上 右手線朝下）急停是否打開？

# 4. 使用 rosrun 執行 按 [ctrl+p] [tab] [tab]
# 格式: rosrun [Package名稱] [執行檔名稱]
rosrun Project ProjectNode

(出現 "兩手臂初始化完成" 在終端機)
(如果終端一直運算但手沒動 可能急停開關沒開 或是 馬達沒連接上)

# 5. 就可以直接開啟新終端機執行你的程式碼
cd Software
source devel/setup.bash
python3 python_publisher.py